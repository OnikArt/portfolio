"use client";
import Link from "next/link"; import { FormEvent,useState } from "react";
export function Contact(){const [notice,setNotice]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setNotice('');const form=e.currentTarget;const data=new FormData(form);try{const res=await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:data.get('name'),phone:data.get('phone'),message:data.get('message'),page:location.pathname,source:'website',utm:location.search})});const body=await res.json() as {error?:string};if(!res.ok)throw new Error(body.error||'Не удалось отправить заявку.');setNotice('Заявка сохранена. Я свяжусь с вами.');form.reset()}catch(error){setNotice(error instanceof Error?error.message:'Не удалось отправить заявку.')}finally{setBusy(false)}}return <section className="contact" id="contact"><div className="container contact__grid"><div><p className="section-label section-label--dark">ЕСТЬ ЗАДАЧА?</p><h2>РАССКАЖИТЕ О ЗАДАЧЕ.<br/>Я ПРЕДЛОЖУ<br/>СЛЕДУЮЩИЙ ШАГ.</h2><p className="contact__copy">Телефон: <a href="tel:+79956691242">+7 995 669-12-42</a><br/><a href="https://t.me/iamartush1an">Telegram · @iamartush1an</a><br/>VK и Instagram — ссылки появятся после подтверждения адресов.</p></div><form className="contact-form" onSubmit={submit}><label>Имя *<input required minLength={2} name="name" autoComplete="name"/></label><label>Номер телефона *<input required name="phone" type="tel" inputMode="tel" autoComplete="tel" placeholder="+7 (___) ___-__-__"/></label><label>Расскажите о задаче<textarea name="message" rows={4} maxLength={3000}/></label><label className="consent"><input type="checkbox" required/> Согласен на обработку данных по <Link href="/privacy">политике конфиденциальности</Link>.</label><button disabled={busy}>{busy?'Отправляем…':'Отправить'} <span>↗</span></button>{notice&&<p className="form-notice" role="status">{notice}</p>}</form></div></section>}
