import { json } from "@/lib/platform";
export async function GET(request:Request){const url=new URL(request.url);const lat=Number(url.searchParams.get('lat')||51.672),lon=Number(url.searchParams.get('lon')||39.1843);try{const response=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&current=temperature_2m,apparent_temperature,weather_code&daily=temperature_2m_max,temperature_2m_min&timezone=auto&forecast_days=3`);if(!response.ok)throw new Error('WEATHER_UNAVAILABLE');return json(await response.json())}catch{return json({error:'Не удалось получить погоду.'},503)}}
