import { notFound } from "next/navigation"; import { requireChatGPTUser } from "@/app/chatgpt-auth"; import { all } from "@/lib/platform"; import { AdminShell } from "@/components/admin/AdminShell"; import { AdminWorkspace } from "@/components/admin/AdminWorkspace";
const titles:Record<string,string>={leads:'Заявки',dialogs:'Диалоги',works:'Мои работы',tariffs:'Тарифы',ratings:'Рейтинги',analytics:'Аналитика',content:'Контент',notifications:'Уведомления',integrations:'Интеграции',settings:'Настройки'};
export default async function Section({params}:{params:Promise<{section:string}>}){const {section}=await params;if(!titles[section])notFound();await requireChatGPTUser(`/admin/${section}`);let rows:Record<string,unknown>[]=[];try{const queries:Record<string,string>={leads:'SELECT * FROM leads ORDER BY created_at DESC',dialogs:'SELECT * FROM conversations ORDER BY updated_at DESC',works:'SELECT * FROM projects ORDER BY featured_order,updated_at DESC',tariffs:'SELECT * FROM tariffs ORDER BY sort_order',ratings:'SELECT * FROM ratings ORDER BY created_at DESC',notifications:'SELECT * FROM notification_events ORDER BY created_at DESC LIMIT 100'};if(queries[section])rows=await all(queries[section])}catch{}return <AdminShell active={`/admin/${section}`}><header className="admin-top"><div><span>ONIKART / ADMIN</span><h1>{titles[section]}</h1></div><a href="/">Вернуться на главную ↗</a></header><AdminWorkspace section={section} initialRows={rows}/></AdminShell>}
