import { notFound } from "next/navigation";
import { Header } from "@/components/layout/Header";
import { Footer } from "@/components/layout/Footer";
import { one } from "@/lib/platform";
type Project={title:string;subtitle:string|null;short_description:string;status:string;service_tags:string};
export default async function Work({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const project=await one<Project>("SELECT title,subtitle,short_description,status,service_tags FROM projects WHERE slug=? AND published=1",slug);if(!project)notFound();const tags=JSON.parse(project.service_tags) as string[];return <><Header/><main className="case-page"><section className="case-hero"><div className="container"><p className="section-label">CASE / SELECTED WORK</p><h1>{project.title}</h1><p>{project.subtitle||project.short_description}</p><p className="project-status"><i/>{project.status==='IN_PROGRESS'?'В работе':'Завершено'}</p></div></section><section className="section case-narrative"><div className="container"><article><span>01</span><h2>ЗАДАЧА</h2><p>{project.short_description}</p></article><article><span>02</span><h2>НАПРАВЛЕНИЯ</h2><p>{tags.join(' · ')||'Digital system'}</p></article></div></section></main><Footer/></>}
