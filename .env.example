# Core — use the pooled Neon PostgreSQL connection string.
DATABASE_URL=
NEXT_PUBLIC_SITE_URL=

# Admin — password must be a bcrypt hash, never plaintext.
ADMIN_EMAIL=
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=

# Telegram — optional until notifications are enabled.
TELEGRAM_BOT_TOKEN=
TELEGRAM_ADMIN_CHAT_ID=

# Storage — server-side token for the project's public Vercel Blob store.
BLOB_READ_WRITE_TOKEN=
